---
id: { UUIDv7 }
---

# {Node name} outcomes

The file is `{slug}.outcome.md` beside `{slug}.spec.md`, repeating the node's own slug. The directory names the output; the record names no output, no assertion, no malleability, no state, no impact, no value, and no threshold.

## Conditions

- {A condition the output moves, stated directionally and without a value — e.g. "A person captures tasks on most active days"} — [metric source]({URL of the metric's authoritative source: the analytics, experimentation, or infrastructure platform that reads it})
- {Another condition} — [metric source]({URL})

## Selection source

{Only when the node holds `.variant` children.} [flag definition]({URL of the flag in the feature-flag system that serves the variants})
