# {Probe name}

The protocol lives at `probes/{probe-slug}/probe.md`, the target of the assertion's `[probe]` tag. Working runs stay in an ignored `runs/` inside this directory; every attested run retains at least one inspectable artifact beside this file.

## Intent

{The operator intent and the uncertainty this probe addresses — the misunderstanding between operator and agent it exposes.}

## Environment and preconditions

{What must be true before the protocol runs: build, data, configuration, accounts, devices.}

## Protocol

1. {Step any Author, person or agent, can execute and repeat.}
2. {Step.}

## Attested run

- Date: {ISO date}
- Observations: {what the run showed, step by step}
- Artifacts: [{recording, screenshot, transcript, log, or structured output}]({artifact filename in this directory})

## Verdict

{The Author's verdict: `passed` or `failed`, with the observation that decides it. Actor provenance comes from the verification run, never from this prose.}

## Limitations

{Material areas the protocol did not exercise, and why.}
